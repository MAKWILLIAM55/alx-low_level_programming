#include "main.h"
#include <string.h>
/**
 * _strlen - The function gives the length of a string
 * @s: string pointer tp pass tp the function
 * Return: returns ;ength of the string
 */
int _strlen(char *s)
{
	return (strlen(s));
}
